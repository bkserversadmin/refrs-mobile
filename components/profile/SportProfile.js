import { withTheme } from "@draftbit/ui";
import { Text, View } from "react-native";

export const SportProfile = (props) => {
	return (
		<View>
			<Text>sport profile</Text>
		</View>
	);
};

export default withTheme(SportProfile);
