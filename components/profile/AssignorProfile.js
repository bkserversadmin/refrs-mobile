import { withTheme } from "@draftbit/ui";
import { Text, View } from "react-native";

export const AssignorProfile = (props) => {
	return (
		<View>
			<Text>assignor profile</Text>
		</View>
	);
};

export default withTheme(AssignorProfile);
